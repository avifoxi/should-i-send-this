<%= form_for(@comment, url: version_comment_path(@version, @comment), method: :post) do |f| %>
  <p>
    <h3>Give Feedback:</h3>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<hr>

<%= @version.content %>